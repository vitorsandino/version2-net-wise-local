# Configuração do Banco de Dados PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=version2_netwise
DB_USER=version2
DB_PASSWORD=changeme_strong_password

# Configuração do Servidor
PORT=3000
NODE_ENV=production
API_URL=http://seu-servidor.com:3000

# Configuração de Segurança
JWT_SECRET=change_this_to_a_very_long_random_string_at_least_64_characters_long
JWT_EXPIRES_IN=7d
ENCRYPTION_KEY=change_this_to_another_very_long_random_hex_string_64_chars_minimum

# Frontend URL (para CORS)
FRONTEND_URL=http://seu-servidor.com
